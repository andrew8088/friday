# ~/friday/config/friday.conf
# Friday configuration
# Copy this file to friday.conf and fill in your values

# TickTick API credentials (get from developer.ticktick.com)
TICKTICK_CLIENT_ID="your_client_id_here"
TICKTICK_CLIENT_SECRET="your_client_secret_here"
TICKTICK_ACCESS_TOKEN=""

# Google Calendar (via gcalcli)
# Leave GCALCLI_ACCOUNTS empty to use default gcalcli config.
# For multiple accounts, each needs its own gcalcli config folder with OAuth credentials.
# To set up a new account:
#   1. Create a folder: mkdir -p ~/.gcalcli/work
#   2. Authenticate: gcalcli --config-folder ~/.gcalcli/work list
#
# Simple format (all calendars): "path:label,path:label"
#   GCALCLI_ACCOUNTS="~/.gcalcli/personal:Personal,~/.gcalcli/work:Work"
#
# JSON format (with calendar filtering):
#   GCALCLI_ACCOUNTS='[{"config_folder": "~/.gcalcli/work", "label": "Work", "calendars": ["Work", "Meetings"]}]'
GCALCLI_ACCOUNTS=""

# Time zone
TIMEZONE="America/Toronto"

# Work hours (when to suggest work tasks)
WORK_HOURS="08:00-17:00"

# Task list categorization (comma-separated TickTick project names)
WORK_TASK_LISTS="Work"
PERSONAL_TASK_LISTS="Personal,Learning"

# Deep work preferences
DEEP_WORK_HOURS="09:00-11:00,14:00-16:00"

# Output directories
# Daily briefings will be saved as YYYY-MM-DD.md in this directory
# If the file already exists, briefing will be appended
DAILY_JOURNAL_DIR=""  # Leave empty to use ~/friday/journal/daily

# Daily recaps will be saved as YYYY-MM-DD.md in this directory
DAILY_RECAP_DIR=""  # Leave empty to use ~/friday/recaps/daily

# How recent a recap must be (in hours) to include in morning briefing
# If yesterday's recap is older than this, only patterns from recent recaps are shown
RECAP_FRESHNESS_HOURS="36"

# Review schedule
WEEKLY_REVIEW_DAY="Sunday"

# Telegram Bot Settings
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=""

# Comma-separated list of Telegram user IDs allowed to use this bot
# To find your user ID, message @userinfobot on Telegram
# Leave empty to allow anyone (not recommended)
TELEGRAM_ALLOWED_USERS=""

# When to send the morning briefing automatically (HH:MM in your timezone)
TELEGRAM_BRIEFING_TIME="06:50"

# Start-of-week plan schedule (day + time)
TELEGRAM_START_WEEK_DAY="Sunday"
TELEGRAM_START_WEEK_TIME="16:00"

# End-of-week review schedule (day + time)
TELEGRAM_END_WEEK_DAY="Friday"
TELEGRAM_END_WEEK_TIME="17:00"

# When to send a recap reminder if no recap exists (HH:MM in your timezone)
TELEGRAM_RECAP_REMINDER_TIME="21:00"
